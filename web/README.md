# Web UI

Vite + React frontend for the crossword agent. See the root README for how to run it.

```bash
npm install
npm run dev      # http://127.0.0.1:5173  (proxies /api to :8000)
npm run build    # writes dist/, served by `python3 -m crossword serve`
```
